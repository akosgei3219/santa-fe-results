# Core SDK. Includes the server-side HTTP stack (starlette + uvicorn) used by
# the streamable-http transport. Pinned below 2.0: mcp 2.x renames FastMCP to
# MCPServer (breaking) — migrate deliberately, not on a fresh race-week build.
mcp>=1.2.0,<2

# Only needed for CLIENTS that connect over HTTP from behind a SOCKS proxy.
# httpx[socks]
